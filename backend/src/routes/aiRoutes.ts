// AI routes removed. This file is retained as a placeholder to avoid import errors
// but AI endpoints have been disabled per project requirements.

import { Router } from "express";
const router = Router();

export default router;
